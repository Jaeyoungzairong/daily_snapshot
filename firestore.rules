rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 각 사용자(uid)는 자기 자신의 데이터만 읽고 쓸 수 있다. 로그인은 구글 계정뿐이라
    // uid만으로 소유권을 구분한다.
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
